import dotenv from "dotenv";
dotenv.config();
import express from "express";

const createApp = () => {
  // Configuration
  const app = express();

  // Routes

  return app;
};

export default createApp;
